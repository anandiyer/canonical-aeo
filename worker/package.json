{
  "name": "canonical-aeo-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node devserver.mjs",
    "test": "node --test test/*.test.js",
    "scan": "node scan.mjs",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail"
  }
}